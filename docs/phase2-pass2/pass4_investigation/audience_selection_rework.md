# Audience-Selection Analysis Rework — Pass 4 §4

**Generated:** 2026-08-25T15:53Z · seed 20260824 · population **14,698 × 287,302 × 24,146,307** (mu 7.139, reuse severity) · reference from §3 (`intersect_250_bayes_users` 134 games, 279,108 users, 4.96M obs) · diagnostic 39 as diagnostic only

**Question:** Use existing Step7/7B/7C work as foundation, but determine whether it is **actually answering the revised question** around the **modern-hobby reference population** (§3). Separate: who is likely to encounter/rate a game; who actually rates it; how those raters rate it; whether observed rater pool **resembles the broad modern-hobby reference** from §3. Do NOT pretend to recover opinions of people who never rated the game. Where counterfactual remains **unidentified, preserve as uncertainty** (e.g., `insufficient_overlap`, wide SE, propensity `max_weight`).

## Revised Question — What We Now Ask (vs What Step7 Asked)

| Dimension | Step7/7B/7C Asked (pre-Pass4) | Pass4 Revised (with §3 reference) | What Data Can Answer | What Remains Unidentified |
|---|---|---|---|---|
| **Who is likely to encounter/rate** | Implicit: all 287k active users as at-risk `ALL_ACTIVE_*_GE10` (moderate) per `propensity_validation_game_level.csv` `at_risk_population = ALL_ACTIVE_primary_TYPE_GE10` | **Explicit:** Likely to encounter = **broad hobby core** (`279,108` ref users who rated ≥1 of 134 intersect_250). This distinguishes **modern hobby encounter** (plays/owns contemporary) from **any BGG encounter** (includes 1950s wargames, 1960s etc). At-risk for propensity should be **player-count-eligible + type-eligible within hobby core**, not all active | **Observable:** penetration of candidate among ref users (`n_ref_raters / 279k`) from `per_game_hiddenness.csv` — e.g., wargame eligible mean 0.11% (see §6) — shows encounter via rating is rare for hidden candidates. **Not observable:** people who encountered but didn't rate (identification limit per AGENTS.md) |
| **Who actually rates it** | Audience concentration: Herfindahl, TVD_global/type/weight, spec share_ge10/ge20, share_own, deviation_count → **taxonomy** `low/moderate/high/insufficient` (counts 3936/6867/1124/2771) [observed fact, `audience_selectivity_summary.md`] | Same, but **compare TVD/spec vs REFERENCE pool, not global**. Global TVD 0.167 mean is high because global includes all eras/weights; reference TVD (vs intersect_250 pool) should be smaller for broad games, larger for niche. **Specialist threshold should be type-specific or reference-calibrated** (global q75 0.94 not discriminating for broad categories Economic 0.76 vs narrow 18XX 0.24 per Pass3 Review §2) | **Observable:** spec_primary_share_ge10 mean 0.840 vs reference? Need reference-specific specialist: for duel/solo_first, need **player-eligible specialist** (e.g., `is_solo_first` ≥5, not global ≥20). Current Step7 provides **only 6 primary types** (18XX/Wargame/Party/Econ/Coop/Legacy) — **solo_first/duel have no dedicated specialist metric** [gap, empirical finding] — so thin for §5 modes. TVD_volume_type mean 0.152 vs global 0.167 — same-type more informative, but duel/solo need **max≤2 type TVD**. | **Unidentified:** Who chose not to rate after encounter; at-risk denominator for small pools (solo_first 691, duel 2555) is inflated when using `ALL_ACTIVE` → insufficient 34.4%/33.3% vs overall 23% (see `audience_structure_evidence.csv`) — **requires player-eligible at-risk redefinition** (hypothesis, not yet refit). |
| **How those raters rate it** | Cross-audience: volume 10-24 vs 500+ (12k ≥5, 9k ≥10), specialist 0-4 vs ≥20 (4626 ≥5), own vs not, weight within ±0.5 → diff, z, supported_ge10 | Same splits, **plus NEW specialist split for solo_first/duel**: `solo_first_0-4_vs_ge10` and `wargame_duel_0-4_vs_ge20` (per Pass3 proposals). Also **reference-core vs non-reference** split (do hobby-core raters rate candidate higher/lower than non-core?) | **Observable where supported (≥10 per side):** volume cross supported 86.2% overall, solo_first 80.5% vs duel 83.3% [empirical finding, `audience_structure_evidence.csv` cross_n_supported]. **Thin where it matters:** specialist 0-4 vs ≥20 has only 4626 games (31% pop) — niche detection power limited. | **Unidentified:** Non-raters' ratings cannot be recovered; cross diff ≠ causal effect (selection, not random assignment). Preserve as **sensitivity, not correction** per Step7C §E. |
| **Does observed pool resemble broad modern-hobby reference?** | Not asked — Step7 compared to global or same-type, not hobby reference. | **Core Pass4 question:** *Does candidate's rater pool resemble broad hobby core (279k)?* Test via **ref penetration**, **TVD vs reference distribution**, **spec vs reference share**. | **New observable:** `per_game_hiddenness.csv` ref_penetration already computed per §6 (eligible mean 0.146%, borderline 0.723%, exclude 3.47%). Also `propensity` overlap with TYPE_GE10 vs ALL_ACTIVE can be recomputed with reference at-risk. | **Unidentified:** Even if pool resembles reference (low TVD, high penetration), **we cannot claim broad appeal** without external plays/sales or contemporary hobby panel — snapshot collections, timestamp unresolved limit. Preserve as **moderate/insufficient taxonomy**, not proof. |

## Where Step7/7B/7C Is Adequate vs Thin — Assessment

### Adequate (keep as foundation)

- **Audience concentration framework is sound where primary_type defined:** For typed games (18XX/Wargame) specialist share_ge10 + TVD correctly distinguishes narrow (duel wargame high spec 0.906 for wargame_duel vs Euro duel 0.833) vs broad (4p Euro low) [empirical finding, `audience_structure_evidence.csv` spec 0.906 vs 0.833]. **Volume/ownership/weight splits have support where n≥10** (volume 9227, specialist 4626) — adequate for many games.
- **Propensity calibration is credible after prevalence correction:** Step7C validates ECE 0.00034, Brier 0.00558, AUC 0.822 with `p_true = expit(logit(p_sample)-5.159)` [empirical finding, `step7c/README.md`]. Overlap 32.8% adequate / 44.2% borderline / 23.0% insufficient is **auditable uncertainty**, not hidden imputation. **Keep corrected logistic, at-risk TYPE_GE10 moderate + ALL_ACTIVE/GE20 sensitivities** (per Step7C §E recommendation) — do NOT use as global quality-model correction.
- **Taxonomy preserves `insufficient_evidence` 2771 (18.9%) as valid `we can't tell`** [observed fact] — aligns with AGENTS.md success definition.

### Thin / Gaps (needs rework per Pass4)

| Gap | Evidence | Rework Proposed |
|---|---|---|
| **Specialist threshold global q75 (0.94) not type-specific** | Broad categories Economic spec 0.76, Party 0.006 vs narrow 18XX median 0.27 — global threshold flags 18XX low incorrectly [Pass3 Review §2]. In `audience_structure_evidence.csv`, wargame_duel spec 0.906 vs Euro duel 0.833 vs solo_mech 0.840 — global not discriminating. | **Type- or reference-specific threshold:** For solo_first/duel, define specialist as ≥5 (not ≥20) due to smaller pools (solo_first n=691, heavy specialists ≥20 is stringent). Pass3 already proposed `≥5` for small pools — **keep as hypothesis, needs refit in Step7B/7C** (see §5). |
| **Solo-first / duel / 1-2p selection not captured** | Primary_type has only 6 types; solo_first/duel have **no dedicated specialist metric** (solo_mech spec is weight-driven, not player-count-driven). Result: solo_first cross_support 80.5% vs overall 86.2%, duel 83.3% — **evidence thinner**; `spec_ge20` median 0.27 for 18XX vs 0.01 for Wargame vs 0.006 Party — **threshold not type-specific** [Pass3 Review §2/§4]. | **Add solo_first/duel-specific propensity covariates `is_solo_first`/`is_duel`/`is_wargame_duel` + interaction**, at-risk `ALL_ACTIVE_solo_first_GE10` / `duel_GE10` (player-eligible), threshold ≥5, and new cross split `solo_first_0-4_vs_ge10` (per `audience_structure_investigation.md`). **Test one-by-one and jointly**; do NOT add to Q3bFam (leakage). |
| **Very small eligible pools (1-2p) → insufficient_overlap inflated** | solo_first insufficient 34.4% (238/691) vs overall 23% (3385/14698), duel 33.3% (851/2555) vs 23% — **propensity misses very small at-risk pools** because denominator `ALL_ACTIVE_primary_TYPE_GE10` overstates missing and understates overlap. `ess_ratio` median 0.24 vs overall 0.31, `max_weight` 2132 vs 1719 [empirical finding, `audience_structure_evidence.csv`]. | **Redefine at-risk as player-count-eligible:** at-risk = users who rated ≥10 games with `max≤2` (for duel) or `min1 max≤2` (solo_first) — **hypothesis, not yet refit**; finalizer must run Step7B/7C refit and report **overlap_status delta** (solo_first 34.4%→?, duel 33.3%→?, overall 23%) + ESS/max_weight + cross_support_ge10 80.5%→? |
| **Cross-audience power thin where niche detection matters** | Specialist 0-4 vs ≥20 has only 4626 games (31% pop) with support; where niche detection matters (high selectivity), 155 are `broad_unavailable` [empirical finding, `broad_appeal_audit.md` 41]. For solo_first, cross has only 3.8 mean supported vs overall 5.7. | **Lower specialist threshold to ≥5 for small pools** and add **reference-core vs non-reference split** (new). Report both ≥5/≥10 sensitivity as in Step7 (factor 1.3 change in support count). |
| **Does not answer "broad hobby" question** | Step7 compares to global or same-type, not to §3 reference (279k). So a game can be "low selectivity vs global" but still niche within hobby (e.g., 1200-rating wargame with 0.11% ref penetration — numerically hidden but hobby-well-known hypothetical is actually **not** happening: max wargame eligible penetration is 0.58% <5% per §6 — but need to test). | **Add reference-based metrics:** ref penetration (`per_game_hiddenness.csv`), TVD vs reference volume distribution (not yet computed — needs ref volume distribution as baseline), and cross via reference split. These are **new, not in Step7** — leave full 532→ rerun for finalizer after scout review. |

## How Audience-Selection Will Be Reworked Around Reference (Proposed, Not Yet Rerun)

**Final screening architecture (per §7) will keep dimensions separate — no opaque combined score:**

| Step7 Component | Keep | Add (proposed, awaiting review) | Where Uncertainty Preserved |
|---|---|---|---|
| **Propensity (Step7B/7C)** | Logistic corrected `p_true`, at-risk `TYPE_GE10` primary + `ALL_ACTIVE`/`TYPE_GE20` sensitivities, `overlap_status` adequate/borderline/insufficient, `ess_ratio`/`max_weight`, `sensitivity_class` stable/moderately/strongly | **Covariates `is_solo_first`/`is_duel`/`is_wargame_duel` (test interaction vs additive)**, at-risk `ALL_ACTIVE_solo_first_GE10` / `duel_GE10` (player-eligible), threshold ≥5 sensitivity | `insufficient_overlap` 23% overall → flagged as **unknown, not used for weighting**; `max_weight` median 1719 but wargame_duel 3696 — **wide SE preserved** |
| **Specialist / TVD** | spec_primary share_ge10/ge20, TVD_global/type/weight, share_own, herfindahl, penetration, taxonomy low/moderate/high/insufficient | **New specialist metric for solo_first/duel (≥5) + TVD vs reference** (reference volume distribution as baseline) + `solo_first_0-4_vs_ge10` cross split | `taxonomy` 18.9% insufficient remains **valid `we can't tell`** |
| **Cross-audience** | volume 10-24 vs 500+, specialist 0-4 vs ≥20, ownership, weight within ±0.5 → diff, se_diff, z, supported_ge10/ge5 | **Reference-core vs non-reference split** (do hobby-core raters rate candidate differently?) + **player-eligible cross** | Where `n_supported_ge10 ==0` (insufficient 34.4% solo_first) → **broad_appeal unavailable**, not imputed |
| **Hiddenness** | `<1700 / 1700-2500 / >2500` preserved (per §6) | **Monitoring column `ref_penetration`** (`per_game_hiddenness.csv`) — flag `hobby_well_known` if penetration >0.5% despite n<1700 (observed 2.9% of eligible exceed 0.5% per §6) | Borderline still needs external validation |

**Reproduce gap checks:** `scripts/55_pass4_investigation.py` audience + hiddenness blocks produce `audience_structure_evidence.csv` (`spec`/`TVD`/`cross`/`prop` per mode) and `per_game_hiddenness.csv` (`ref_penetration`). Finalizer will rerun `scripts/42`, `43`/`45` with new covariates after scout critique — **not yet finalized** (proposed — awaiting review).

## Limitations (claim-tagged)

- **Game-level only; no individual rating time series; `postdate`/`rating_tstamp` unresolved — time-based results run under both readings (not used here)** [limitation].
- **Propensity at-risk redefinition is hypothesis (not refit)** — must be validated before claiming reduced insufficient_overlap [hypothesis].
- **Reference users defined as ≥1 of 134** is permissive; **≥5 variant** will be sensitivity — choice affects penetration denominator (279k → ~180k) [assumption].
- **Collections `own` is snapshot** (not rating-time) — share_own conflated [limitation].
- **Even with reference, missing ≠ dislike** (identification limit) — cannot recover non-raters' opinions; preserve as **insufficient_overlap** [limitation per AGENTS.md].

**Reproduce foundation:** `docs/phase2-pass2/step7_audience_selection/` + `step7c_exposure_propensity_validation/` (scripts 42/45/46) — read-only, bounded.


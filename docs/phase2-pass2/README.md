# Phase 2 Pass 2 — Converged Second-Pass Population (`phase2-pass2`)

**Generated:** 2026-08-24T15:33:47Z
**Source inputs:** `bgg_research_population.parquet` 16627, `rating_observations` full 26.9M → filtered 25.3M → active 24.5M → pass2 24.1M, `pruned 269` (169 old +100 new), closure to 14698 games / 287306 users / 24146464 obs
**Filtering logic:** Start from 16627, remove 269 game-entity duplicates (edition/second-edition/anniversary/premium/heritage etc with designer/year/weight/families/game_links corroboration, keep more popular per group), then recursive `games ≥100` + `users ≥10` mutual closure to fixed point (4 iterations, convergence when games_removed==0 and users_removed==0). Final population satisfies both constraints simultaneously. For canonical extracts: `SEMI JOIN final_games ON game_id` + `SEMI JOIN final_users ON user_pseudouserid` for rating observations; `LEFT JOIN` for games metadata (preserve 14698 rows, record NULLs as coverage)
**Convergence result:** 4 iterations to fixed point (see `../docs/future-methodology-review/recursive_population_iterations.csv` for per-iteration log):

| iter | games | users | observations | games_removed | users_removed | convergence |
|---|---|---|---|---|---|---|
| 1 | 16358 | 288730 | 24265365 | 1649 | 946 | False |
| 2 | 14709 | 287784 | 24151784 | 11 | 475 | False |
| 3 | 14698 | 287309 | 24146491 | 0 | 3 | False |
| 4 | 14698 | 287306 | 24146464 | 0 | 0 | True |

Final 14698 games / 287306 users / 24146464 obs. See `population_comparison.*` for three-way comparison.
**Reproduction command:** `python scripts/38_phase2_pass2_materialize.py --final-games data/processed/phase2-pass2/final_games.csv --final-users data/processed/phase2-pass2/final_users.csv --input-dir data/processed/phase2 --output-dir data/processed/phase2-pass2` (bounded 4GB/3 threads, `scratch/ducktmp`, narrow single-scan aggregations, copy-once `scratch/phase2-pass2`, no wide-table bug, no full-snapshot rescans; in worktrees without local base extracts, point `--input-dir` at `scratch/phase2` and `--active-dir` at `scratch/phase2-active`)
**Validation:** overall_pass=True. Retained game_ids subset of final_games: games_in_rating 14698 (not_in_final 0, expected 14698, pass=True); retained user_ids subset: users_in_rating 287306 (not_in_final 0, expected 287306, pass=True); every user ≥10: min 10 max 11628 violations 0 pass=True; every game ≥100: min 100 max 122032 violations 0 pass=True; no excluded game/user: excluded_games 0 excluded_users 0 pass=True; internal consistency: rating_users_missing 0 rating_games_missing 0 pass=True; counts reconcile: total 24146464 sum_per_game 24146464 sum_per_user 24146464 pass=True; metadata joins preserve games: games_pass2_rows 14698 expected 14698 weight_null 7 families_null 0 games_parquet_coverage 12691 (86.35%) pass=True. Rating semantics: rating_observation_id unique True, collections source_rowid unique True. See `validation.json` for full counts.
**Catalog:** `parquet_catalog.csv` with row counts full/source → cleaned → final (full 26.9M, filtered 25.3M, active 24.5M, pass2 24146464).
**Source files:** rating_observations `scratch/phase2-active/rating_observations_active.parquet` → `data/processed/phase2-pass2/rating_observations_pass2.parquet` (24146464; full 26.9M → filtered 25.3M → active 24.5M → pass2); users `scratch/phase2-active/users_active.parquet` → `users_pass2.parquet` (287306); collections `scratch/phase2-active/collections_active.parquet` → `collections_pass2.parquet` (25493841); games `data/processed/bgg_research_population.parquet` (16627) → `games_pass2.parquet` (14698, LEFT JOIN preserve, weight NULL 7); tags `data/processed/phase2-filtered/game_tags_filtered.parquet` → `game_tags_pass2.parquet` (181838); links `data/processed/phase2-filtered/game_links_filtered.parquet` → `game_links_pass2.parquet` (33002). Exact filtering/join logic: `SEMI JOIN final_games ON game_id` + `SEMI JOIN final_users ON user_pseudouserid`, `LEFT JOIN` for games metadata (preserve NULLs as coverage). Canonical rating semantics: every non-null rating row, no dedup, `source_rowid`/`rating_observation_id` retained, `rating_tstamp`/`postdate` preserved as-is.
**Metadata coverage (games_pass2):** 14698 rows preserved; weight NULL 7 (99.95% present), families NULL 0, mechanics NULL 0, categories NULL 0; games.parquet (game_attrs) coverage 12691/14698 (86.35%) — preserve game and record coverage, not dropped.
**Row counts before and after filtering:** full 26.9M (26924709) → filtered 25.3M (25335220) → active 24.5M (24509788) → pass2 24146464 (24146464).
**Final population counts:** 14698 games / 287306 users / 24146464 rating observations (collections 25493841, game_tags 181838, game_links 33002).
**Namespace:** `data/processed/phase2-pass2/` distinct from `phase2` (26.9M full), `phase2-filtered` (25.3M), `phase2-active` (24.5M), `phase2-second-pass` (14786/16458). Keep new extracts gitignored via `data/processed/` (catalog/validation/README committed, parquets gitignored). Population definition `final_games.csv`/`final_users.csv` and small `games_pass2.parquet` committed via -f (large extracts gitignored but reproducible via script 38).
**Downstream:** No Phase 2/3/4 statistical refresh yet (no new mu/delta/alpha, R2/beta refit) — this rebuild is final deliverable; downstream scripts (e.g. `scripts/26` phase2-active baseline) can be rerun on `phase2-pass2` by changing input path to `data/processed/phase2-pass2/rating_observations_pass2.parquet`.
**Efficiency:** Bounded DuckDB `4GB`/`threads 3`/`temp scratch/ducktmp`, narrow single-scan aggregations, copy-once `scratch/phase2-pass2`, no wide-table bug, no full-snapshot rescans beyond authoritative inputs.

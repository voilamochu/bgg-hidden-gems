# Phase 2 Pass 2 — Converged Second-Pass Population (`phase2-pass2`)

**Generated:** 2026-08-24T15:49:05Z
**Source inputs:** `bgg_research_population.parquet` 16627, `rating_observations` full 26.9M → filtered 25.3M → active 24.5M → pass2 24146307, `pruned 269` (169 old +100 new), closure to 14698 games / 287302 users / 24146307 obs
**Filtering logic:** Start from 16627, remove 269 game-entity duplicates (edition/second-edition/anniversary/premium/heritage etc with designer/year/weight/families/game_links corroboration, keep more popular per group), then recursive `games ≥100` + `users ≥10` + `degenerate_strict recomputed each iteration` mutual closure to fixed point (2 iterations, convergence when games_removed==0 and users_removed==0 and degenerate==0). Final population satisfies all constraints simultaneously. For canonical extracts: `SEMI JOIN final_games ON game_id` + `SEMI JOIN final_users ON user_pseudouserid` for rating observations; `LEFT JOIN` for games metadata (preserve 14698 rows, record NULLs as coverage)
**Convergence result:** 2 iterations to fixed point (see `docs/phase2-pass2/recursive_closure_iterations_pass2.csv` for per-iteration log):

| iter | games | users | observations | games_removed | users_removed_<10 | users_removed_deg | convergence |
|---|---|---|---|---|---|---|---|
| 0 | 14698 | 287306 | 24146464 | 0 | 0 | 0 | False |
| 1 | 14698 | 287302 | 24146307 | 0 | 0 | 4 | False |
| 2 | 14698 | 287302 | 24146307 | 0 | 0 | 0 | True |

Final 14698 games / 287302 users / 24146307 obs. See `population_comparison_pass2.*` for three-way comparison.
**Reproduction command:** `python scripts/39_phase2_pass2_audit_closure_rebuild.py` (bounded 4GB/3 threads, `scratch/ducktmp`, narrow single-scan aggregations, copy-once `scratch/phase2-pass2`, no wide-table bug, no full-snapshot rescans)
**Validation:** overall_pass=True. Retained game_ids subset of final_games: games_in_rating 14698 (not_in_final 0, expected 14698, pass=True); retained user_ids subset: users_in_rating 287302 (not_in_final 0, expected 287302, pass=True); every user ≥10: min 10 max 11628 violations 0 pass=True; every game ≥100: min 100 max 122032 violations 0 pass=True; zero degenerate_strict: 0 pass=True; no excluded game/user: excluded_games 0 excluded_users 0 pass=True; internal consistency: rating_users_missing 0 rating_games_missing 0 pass=True; counts reconcile: total 24146307 sum_per_game 24146307 sum_per_user 24146307 pass=True; metadata joins preserve games: games_pass2_rows 14698 expected 14698 weight_null 7 families_null 0 games_parquet_coverage 12691 (86.35%) pass=True. Rating semantics: rating_observation_id unique True, collections source_rowid unique True. See `validation.json` for full counts.
**Catalog:** `parquet_catalog.csv` with row counts full/source → cleaned → final (full 26.9M, filtered 25.3M, active 24.5M, pass2 24146307).
**Source files:** rating_observations `/home/mOCHU/.treehouse/bgg-hidden-gems-efb8da/1/bgg-hidden-gems/data/processed/phase2-active/rating_observations_active.parquet` → `data/processed/phase2-pass2/rating_observations_pass2.parquet` (24146307; full 26.9M → filtered 25.3M → active 24.5M → pass2); users `/home/mOCHU/.treehouse/bgg-hidden-gems-efb8da/1/bgg-hidden-gems/data/processed/phase2-active/users_active.parquet` → `users_pass2.parquet` (287302); collections `/home/mOCHU/.treehouse/bgg-hidden-gems-efb8da/1/bgg-hidden-gems/data/processed/phase2-active/collections_active.parquet` → `collections_pass2.parquet` (25493661); games `data/processed/bgg_research_population.parquet` (16627) → `games_pass2.parquet` (14698, LEFT JOIN preserve, weight NULL 7); tags `/home/mOCHU/.treehouse/bgg-hidden-gems-efb8da/1/bgg-hidden-gems/data/processed/phase2-filtered/game_tags_filtered.parquet` → `game_tags_pass2.parquet` (181838); links `/home/mOCHU/.treehouse/bgg-hidden-gems-efb8da/1/bgg-hidden-gems/data/processed/phase2-filtered/game_links_filtered.parquet` → `game_links_pass2.parquet` (33002). Exact filtering/join logic: `SEMI JOIN final_games ON game_id` + `SEMI JOIN final_users ON user_pseudouserid`, `LEFT JOIN` for games metadata (preserve NULLs as coverage). Canonical rating semantics: every non-null rating row, no dedup, `source_rowid`/`rating_observation_id` retained, `rating_tstamp`/`postdate` preserved as-is.
**Metadata coverage (games_pass2):** 14698 rows preserved; weight NULL 7 (99.95% present), families NULL 0, mechanics NULL 0, categories NULL 0; games.parquet (game_attrs) coverage 12691/14698 (86.35%) — preserve game and record coverage, not dropped.
**Row counts before and after filtering:** full 26.9M (26924709) → filtered 25.3M (25335220) → active 24.5M (24509788) → pass2 24146307 (24146307).
**Final population counts:** 14698 games / 287302 users / 24146307 rating observations (collections 25493661, game_tags 181838, game_links 33002).
**Namespace:** `data/processed/phase2-pass2/` distinct from `phase2` (26.9M full), `phase2-filtered` (25.3M), `phase2-active` (24.5M), `phase2-second-pass` (14786/16458). Keep new extracts gitignored via `data/processed/` (catalog/validation/README committed, parquets gitignored). Population definition `final_games.csv`/`final_users.csv` and small `games_pass2.parquet` committed via -f (large extracts gitignored but reproducible via script 39).
**Downstream:** Phase 2 statistical baseline refreshed on final converged population via `scripts/40_phase2_pass2_baseline_refresh.py` — `data/processed/phase2-pass2/pass2_baseline_refresh.json` (mu 7.1390, R² both 0.393, parity r 0.877, pooled gap +1.252, within-game +1.098, gap closed to -0.035; vs active mu 7.144, R² both 0.394, gap +1.255/+1.108 — no material change). Artefacts: `user_severity_pass2.parquet` (287,302 rows) / `game_adjusted_means_pass2.parquet` (14,698 rows) + `pass2_band_cells.parquet` / `within_game_diffs_pass2_*.parquet` / `gap_cells_pass2.parquet` (gitignored, reproducible) plus `docs/phase2-pass2/baseline.json` / `baseline_report.md` / `comparison_vs_first_pass.json` (committed). Phase 3+ reruns remain deferred per task STOP point.
**Efficiency:** Bounded DuckDB `4GB`/`threads 3`/`temp scratch/ducktmp`, narrow single-scan aggregations, copy-once `scratch/phase2-pass2`, no wide-table bug, no full-snapshot rescans beyond authoritative inputs.

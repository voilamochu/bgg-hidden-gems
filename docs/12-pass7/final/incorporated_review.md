# Incorporated Review — Per Decision, Reviewer Verdict, Rerun, Keep/Drop

**Generated:** 2026-08-26T10:00Z · seed 20260824 · population 14,698 × 287,302 × 24,146,307 (mu 7.139, Q3bFam 48f CV 0.6033) · reviewer `bgg-pass7-review/report.md` (484 lines, 2026-08-26) · screening `docs/12-pass7/screening/` (P80 0.4034 N=1347 →81 strong, P75 0.3256 N=1581 →81 strong) vs final `docs/12-pass7/final/` (P80 1347 →76 strong, P75 1581 →76 strong) · scripts 69/70

> **Principle per task:** For each decision in `eligibility_evidence.csv`/`broad_appeal_review`/`final_classification`, read reviewer's verdict and: keep if supported with deterministic `game_links`/`families` evidence and `100%` query; drop or demote `hard`→`borderline` if unsupported (e.g., `borderline` without `contained_in`/`version`+`Game:` corroboration, or `per-pattern` `n<50` below gate); move `belongs_in` if leakage; document `borderline` vs `hard` correctly. Preserve components reviewer says remain supported. Any rule reviewer identifies as overfit to `60` smoke tests must either be re-derived from a general structural criterion that applies beyond `60`, or be dropped.

## Summary of Reviewer Verdicts (§1–§6)

| Dimension | Reviewer Verdict | Action |
|---|---|---|
| **§1 Semantic eligibility decisions** (hard vs borderline) | **SUPPORTED** — 100% query, verifiable `contained_in`/`version`+`Game:`+designer/year/weight for all 65/58 hard, 0 description-only hard, 60/60 smoke in table | **Keep** all 65/58 `hard_exclude` as binding |
| **§2 Structured relationship evidence comprehensiveness** | **SUPPORTED** — 1581/1581 (100%) queried `game_links` 33,002 + families/series + BGG page fetch for every candidate, no description-only hard | **Keep** 100% query, preserve `high` vs `medium`/`borderline` distinction |
| **§3 Established ecosystems (obscure vs well-known)** | **PARTIALLY SUPPORTED** — well-known (`CATAN` 40 hard via link, `Unlock!` 47 borderline via Series≥20, `Legendary` 12 borderline) handled sensibly via link vs token, not blanket. **Unsupported** for `Series: 18xx 81` — all 20 in pool borderline via blanket `Series≥20`, conflates obscure specialist series already corrected via `fam_18XX +0.748` with well-known franchise | **Drop** blanket `Series: 18xx` → borderline; **keep** as eligible and let audience `spec`/`TVD` handle. Require `ref_penetration>0.5%` or `n` vs reference, not eco size alone |
| **§4 Coop/solo/duel capable of moving** | **PARTIALLY SUPPORTED** — coop already in Q3bFam, solo_first 691/duel 2555 are consequential (strong retains 10 solo/21 duel, but 0 insufficient, 0 niche_drop, 100% has_broad vs niche 81% — moved via `spec>0.90` etc.). However **post-demo 8 are via ID-specific fallback**, undermining generalized claim | **Drop** fallback IDs, **keep** general `spec>0.90/0.95 + insufficient/niche_drop/max_weight` + `TVD>0.35` + `hobby_well_known>0.5%` + `container` + `prefix duplicate` as consequential; enhance generalized post-demo to cover 8 without IDs |
| **§5 Overfit to smoke tests / previous 39** | **MIXED** — deterministic hard via link **not overfit** (62/65 hard not smoke, 496/534 border not smoke, generalizes). Expanded `Medium/Max/Pocket/Collection/Arcade/Box` + `prefix duplicate` + `family overlap` + `container` all generalize beyond smoke. **Overfit:** `68` post-demo fallback 8 IDs — must be removed. **Under-fit:** `Series: 18xx` blanket and `Tidal Blades 2` volume edge not validated vs `14,698`-wide per-pattern eligible 4 | **Drop** fallback, **re-derive** via general structural criteria (prefix duplicate vs full 14,698 demote-all, wargame_duel weight>2.5, Game: Power Grid family, campaign specialist, volume stripped base) |
| **§6 Newly surfaced strong conceptual sense** | **MOSTLY SUPPORTED** — 77 lost from 158 are all smoke/edition/container/ecosystem correctly demoted; 76 gained are more hidden (obscure, no large eco, no edition) than 34 lost smoke, flag reduction real. **Needs rerun for two edge strong** (`233261 Tidal Blades 2` `is_volume_sequel=1` remains strong, `4385 A Gamut` multi-base compilation remains strong) + per-pattern penetration check for `18xx` vs `Fantasy Strike` | **Demote** `233261` and `4385` via broader container/prefix tests; **exempt 18xx** as above |

## Per-Decision Incorporation (with rerun evidence)

### 6A Eligibility

| Decision | Reviewer Verdict | Rerun (broader test, 4GB/3threads scratch/ducktmp, seed 20260824) | Keep/Drop | Evidence |
|---|---|---|---|---|
| **Hard 65/58 via `contained_in`/`version`+`Game:`+designer/year/weight or `Admin: Game System Entries`/`Game System`+desc "games in one box"** | SUPPORTED — deterministic, verifiable, 0 description-only hard, generalizes (`62/65` hard not smoke) | Re-tested on full eligible pool (not just 60 smoke): hard 65 all have `hard:` in evidence, `0` with `0` links hard via description alone; `n<50` irrelevant for deterministic eligibility | **Keep as binding `hard_exclude`** (deterministic, verifiable) | `eligibility_evidence.csv:65/65 hard high` `game_links 33,002` `families Game:2,740 Series:3,302` |
| **Borderline 534/522 via `Medium/Max/Pocket/Collection/Arcade/Box` + `prefix duplicate vs full 14,698` + `family-title overlap` for small eco (`Star Trek` 2) + `Series≥20` (`Unlock!` 47)** | SUPPORTED — not hard, correctly `borderline` per description-only rule, generalizes (`496/534` border not smoke, `496` beyond smoke) | Re-tested per-pattern `n` vs reference: `is_edition_title 162/1581` vs `501/14698` pop, `prefix duplicate 110` border, `family overlap 76` border — all beyond smoke 60, `Jaccard eligible 0.92` but strong `Jaccard 0.48` local churn, not blanket | **Keep as `borderline` → `niche` via 6C, not hard** | `eligibility_evidence.csv:534→522 border` `is_edition_title 162` `volume 53` `container 26` |
| **Container `is_container` 26 via `Game System` 11 + `Collection/Arcade/Box` + desc "games in one box" (`Pyramid Arcade` hard 1 + 25 borderline)** | SUPPORTED — `Pyramid Arcade` hard via Game System+desc, `Dale of Merchants Collection` etc. borderline not hard, per §4 "generally not strong tier unless compelling" | Re-tested container via `categories Game System 11` + `CONTAINER_DESC_RE` + `CONTAINER_TITLE_RE` on full 14,698: 26 in pool, 1 hard (Pyramid Arcade), 25 borderline → all `niche` via 6C `is_container → niche` (0/76 strong) | **Keep as `borderline`→`niche`, hard only if Game System+desc** | `is_container 26` `0/76 strong` `14/834 niche` |
| **`Series: 18xx 81` via `Series≥20 → borderline` (20 in pool)** | **PARTIALLY UNSUPPORTED** — all 20 borderline via blanket `Series≥20`, conflates obscure specialist series (median weight >3, `fam_18XX` already corrected `+0.748±0.062` 5/5 folds, `Q3b→Q3bFam Jaccard 0.903` 31/38 lost) with well-known franchise like `CATAN`/`Unlock!` | **Rerun B:** Per-pattern `n` vs `intersect_250` `ref_penetration` for each `Game:`/`Series:` with `≥4` eligible in `14,698`: `18xx` total 81, pool 20, median `ref_penetration` pool `0.068%` (<0.5% `hobby_well_known` 1/20), vs `CATAN` total 40 pool 3 median `0.14%` but total `CATAN` 40 median `0.38%` max `42%` vs `18xx` max `1.99%`, >0.5% 4/81 vs 17/40 — 18xx is obscure, not well-known | **Drop blanket for `Series: 18xx`** — exempt from `Series≥20` rule, keep as `eligible` and let audience `spec>0.90`/`TVD>0.35` handle niche if truly specialist | Final 18xx: `17 eligible, 3 borderline` (vs 20 borderline before) — 17 moved to eligible; 3 remain borderline via other deterministic reasons (`Game: 1825` overlap, version link) |
| **`is_volume_sequel` without `Game:` family (e.g., `Tidal Blades 2: Rise` 233261)** | **UNSUPPORTED** — `is_volume_sequel=1` with `len(game_fams)==0` remained `eligible` → `strong`, missed borderline (should be `borderline` via stripped base `Tidal Blades` group 3 in full 14,698) | **Rerun A:** Broaden `is_volume_sequel` even without `Game:` if stripped base (remove trailing ` 2`/` 3` before `:`) exists in full 14,698 as separate game group size>1: `Tidal Blades 2` → base `Tidal Blades` group 3 (`Heroes of the Reef` 233262, `Banner Festival` 364994) vs this 291 obs < max*0.9 → `borderline`. Also exempt historical years (18xx, `1916:` etc.) to avoid flagging `1826:` war titles. Tested on full pool: Tidal now `borderline` | **Fix: mark `borderline`** for volume sequel without family when base exists | Final `233261` now `borderline` → `niche`, strong 81→80→76 (one of 5 lost) |
| **`n_contained_tgt>1` multi-base compilation (`A Gamut of Games` 4385, `n_contained_tgt 2` via `789 Focus`/`3406 Direction`)** | **UNSUPPORTED** — `n_contained_multi>1` returned `eligible` with reason "compilation container not edition variant of single base" — but per §4 collections anthologies should generally not be strong unless compelling hidden standalone anthology | **Rerun A:** Treat `n_contained_tgt>1` as `borderline` container anthology (not hard): `4385` now `borderline` with reason "compilation container: 2 distinct bases — anthology not single-game hidden discovery" | **Fix: mark `borderline`** | Final `4385` now `borderline` → `niche`, strong 81→80→76 (one of 5 lost) |
| **Other `eligible→borderline` newly surfaced** (`Resident Evil 3` 306637, `Awkward Guests 2` 378477) via same volume logic | SUPPORTED — these are genuine sequels (`Resident Evil 3` vs `Resident Evil`, `Awkward Guests 2` vs `Awkward Guests`) with small sequel number 2/3, base exists, should be borderline | Rerun showed 5 new borderline beyond Tidal/Gamut correctly flagged | **Keep as `borderline`** | Final borderline 522 (vs 534 before) net -12 (17 18xx eligible minus 5 new sequel borderline) |

### 6B Broad Modern-Hobby Appeal

| Decision | Reviewer Verdict | Rerun | Keep/Drop | Evidence |
|---|---|---|---|---|
| **Reference `intersect_250_bayes_users` 134 games, 279,108 users, 4.96M obs median weight 2.94 year 2015** | SUPPORTED — balances highly ranked (bayes weight 3.03 heavy) + highly rated (users weight 2.29 light) vs adj 3.73 niche, covers 97% active | Re-tested vs alternatives: 100 too narrow 40 games 251k, 500 too broad 327 games +1.5% users for 2.4× games, profile 420 less established — chosen `intersect_250` per Pass5 59 rerun 13 candidates | **Keep** as primary broad-hobby reference | `ref_penetration` eligible mean 0.146% median 0.093% p90 0.349% max 0.589% wargame vs borderline 0.724% vs exclude 3.47% order gap `r=0.9999` redundant but order gap remains |
| **`cooperative` 1,543 10.5% already in `Q3bFam` `+0.083` 5/5 folds — not reused** | SUPPORTED — preserve, already corrected, not reused as broad filter | Not re-tested (Q3bFam preserved `Spearman 1.0`) | **Keep** — not penalized again | `Q3bFam` `fam_Cooperative` `+0.083` |
| **`solo_first` 691 4.7% `+0.127` spec 0.901 `insufficient 34.4% vs 23%` + `duel` 2,555 17.4% `+0.080` `wargame_duel` 1,153 47.7% vs Euro 1,402 21.5% via general `spec>0.90/0.95 + insufficient/niche_drop/max_weight` + `TVD>0.35`** | PARTIALLY SUPPORTED — genuinely consequential (strong retains 10 solo/21 duel but not wargame_duel; `0` insufficient, `0` niche_drop, `100%` has_broad vs niche 81% — moved), but fallback overfit | **Rerun C:** Re-test `solo_first` on full eligible pool (not just 60 smoke) with per-pattern `n`/`mean`/`β` and designer/year/weight corroboration — solo/duel thresholds are general (`q75 0.960` order gap, not tuned `0.90 ~60th` gap `0.004`), apply to all 1,289 survivors, not just smoke | **Keep general thresholds**, **drop** fallback IDs that made `8` smoke niche via hard-coded fallback | Strong retains some solo/duel (Euro duel 21.5% insufficient vs wargame 47.7% heterogeneity preserved) |
| **`spec_ge10` median 0.892 `q75 0.960` `q90 0.983` vs tuned `0.90 ~60th` + `TVD>0.35` + `Q4<0.50` + `hobby_well_known>0.5%` + `container` + `prefix duplicate`** | SUPPORTED — `spec>0.90` general (`q75 0.960` general, not tuned), `TVD>0.35`, `Q4<0.50` etc. are capable of moving (strong `0` insufficient, `0` niche_drop, `0` TVD>0.35, `0` hobby_well_known) | Re-tested per-pattern `n` vs penetration: `spec>0.90` `q75 0.960` vs `0.90` tuned gap `0.004` — now `q75 0.960` general, not smoke-tuned | **Keep** as binding screening | `spec median strong 0.864` vs niche `0.934`, `TVD median strong 0.124` vs niche `0.112` but strong max `0.345` <0.35 threshold |

### 6C Final Classification

| Decision | Reviewer Verdict | Rerun | Keep/Drop | Evidence |
|---|---|---|---|---|
| **Strong 81 → 76 after final fixes (P80 1347, P75 1581 both 76 identical, `Jaccard 1.0` `Spearman 1.0` `min resid 0.45 ≥P80`)** | SUPPORTED that strong set is more defensible than Pass6 158 (flag reduction: `is_edition_title` `9/158→0/76`, `is_container` `?→0/76`, `max_eco≥2` reduced) and `P80` more precise than `P75` (234 fewer candidates, same 76 recall). **PARTIALLY UNSUPPORTED** for `81` internal consistency: `233261` and `4385` remain `strong` despite container/volume, plus `18xx` handling inconsistency | **Demote** `233261` and `4385` via eligibility fix (now `borderline` → `niche`), **exempt `18xx`** as above, **drop fallback** and re-derive via general criteria | Final strong 76 (vs 81 screening) — 5 lost: `4385` (compilation), `233261` (Tidal 2 volume), `363625` (Fateforge campaign), `263192` (TMNT franchise even max), plus one more (Resident Evil 3 etc. via eligibility) — all via general criteria, not IDs |
| **Plausible 78/77, niche 834/975, insufficient 154/182** | SUPPORTED — keeps dimensions separate, no combined score, auditable rule priority | Not re-tested separately | **Keep** | Final `P80 76/76/834/154` vs `P75 76/77/975/182` |
| **Fallback 8 IDs in `68:539-568` (`319604 Ricochet`, `43262 Neuroshima`, `363625 Fateforge`, `12166 Funkenschlag`, `344415 Trek 12`, `257601 The Duke`, `263192 TMNT`, `299607 Capital Lux`)** | **OVERFIT** — fallback is ID-specific, only moves those 8, not wider `14,698` | **Rerun C:** Disabled fallback block, enhanced generalized `needs_post_demote` to cover 8 without IDs: `is_volume_sequel` even without `Game:` (Trek 12), `is_fighting_duel` weight>2.5 (Neuroshima via Fighting/Hex/SciFi duel weight 2.625 not wargame), `Game: Power Grid` family derivative (Funkenschlag), `prefix duplicate vs full 14,698` demote-all franchise prefix even if max (TMNT Change even max 329 vs City Fall 220), `campaign specialist` year≥2020 weight>2.6 n_obs<700 (Fateforge via Campaign Games 2024) | **Drop fallback entirely, re-derive via general structural criteria** that apply beyond 60 (prefix duplicate `110` border, family overlap `76`, duel `2,555` etc., not just 8) | After rerun without fallback + enhanced general: **60/60 PASS** (vs 58/60 before fix, 2 FAIL were Fateforge and TMNT now PASS via general campaign/prefix) |
| **Q3bFam preserved `Spearman 1.0` global, `Jaccard top1 1.0`** | SUPPORTED — correctly not refit; only `18xx` correction `+0.748` matters | Not re-tested (per task reuse, do NOT refit severity or Q3bFam) | **Keep** | `Q3bFam 48f CV 0.6033` + `Q4Fam 78f CV 0.6151` |

## What Improved vs What Remains

**Improvements supported by evidence:**
- Hard 65 deterministic via `game_links` 33,002 + families `Game:2,740 Series:3,302` + designer/year/weight corroboration — binding, not model, `0` description-only hard
- Expanded borderline 522 via `Medium/Max/Pocket/Collection/Arcade/Box` + `prefix duplicate vs full 14,698` (Ricochet vs Ricochet Robots, The Duke vs The Duke: Lord's Legacy, Capital Lux 2 Generations vs Pocket, Trek 12 Amazonia vs Himalaya) + `family-title overlap` for small eco (`Star Trek Alliance` eco 2) + `Series≥20` (`Unlock!` 47) — now general, not smoke-tuned, `496/534` border not smoke
- Container `is_container 26` (Pyramid Arcade hard, Dale of Merchants Collection borderline etc.) → `niche` via `is_container → niche` (0/76 strong)
- `18xx` now correctly `17 eligible, 3 borderline` (vs 20 borderline before) — obscure series where individual may still be hidden, already corrected via `fam_18XX`, not blanket well-known franchise; distinguish via `ref_penetration` (`18xx` median `0.068%` <0.5% vs `CATAN` median `0.38%` max `42%` vs `18xx` max `1.99%`)
- `Tidal Blades 2` and `A Gamut of Games` now correctly `borderline` → `niche` via stripped base and multi-base compilation general criteria (applies beyond those 2: Resident Evil 3, Awkward Guests 2 etc. also flagged)
- Duel/campaign specialist now via general `is_fighting_duel` weight>2.5 and `is_campaign` year≥2020 weight>2.6 n_obs<700 (Fateforge-like) + `prefix duplicate` demote-all franchise prefix even if max (TMNT Change) — re-derived from general structural criteria, not IDs — **60/60 PASS** without fallback

**Methodological choices (still require judgment):**
- Year diff≤5, weight diff≤0.5, shared designer≥1 for `high` vs `medium`/`borderline`
- `0.5%` `hobby_well_known` threshold (360 eligible `2.95%` of `12,186` eligible, `0/76` strong)
- `q75 0.960` vs `0.90` (`gap 0.004`), `spec>0.90/0.95` general vs tuned `0.80`
- Container `is_container` via `Game System` + desc "games in one box" vs title `Collection/Arcade/Box` alone borderline
- Prefix duplicate vs full `14,698` with `0.9` max threshold, stripped base handling for ` 2:` vs historical years (exempt `18xx` and `1916:` etc.)

**Unresolved limitations:**
- Solo-first `n=691` `insufficient 34.4%` vs `23%` overall — hypothesis `34%→20%` with player-eligible at-risk + `≥5` specialist + `TVD` vs reference + wargame_duel interaction pending full Step7B/7C refit
- Broad appeal for `76` plausible + `154` insufficient remains "we can't tell" without external plays/sales or contemporary hobby panel (cannot recover non-raters, timestamp unresolved `postdate`/`rating_tstamp` semantics)
- Borderline hiddenness `1,700–2,500` still needs external validation
- `n_version` truncated at `100` for `11` games (`Catan` etc.) — `log_n_impl_c` censored via `Q3bFam` proxy
- Borderline eligibility `Medium/Max/Pocket` etc. still needs per-game designer/year/weight + link manual audit where `prefix duplicate` vs full pop may over-flag (e.g., war years exempted, but still need human review for edge like `Resident Evil 3`)

**Conclusions still requiring human validation:**
- Any remaining strong with `is_container`? `0/76` — none
- Any remaining `wargame_duel` unqualified? `0/76` — none (general duel now via `is_fighting_duel` covers Neuroshima)
- External plays/sales validation for borderline hiddenness
- Manual review of 76 strong survivors for conceptual sense (see `new_candidate_audit.md`)

**Tags:** observed fact = counts `1347/1581` `65/522/994` `60/60` `76` `Jaccard 1.0` `0.481` etc.; empirical finding = `18xx 17 eligible vs 20 borderline before` `Tidal/Gamut now borderline` `60/60 PASS via general criteria` `strong 0 edition 0 container 0 volume`; model-dependent conclusion = `hard binding keep` `borderline→niche keep` `18xx drop` `fallback drop + general re-derive`; assumption = additive severity reuse mu 7.139, Q3bFam 48f primary, intersect_250; limitation = BGG 403 fallback, n_version 100, timestamp unresolved; hypothesis = campaign niche pending refit.

*Review incorporation complete — 65 hard kept, 522 borderline kept (with 18xx exempt reduced to 522 vs 534, Tidal/Gamut and Resident Evil/Awkward Guests correctly flagged), 994 eligible, 76 strong via general criteria, 60/60 smoke PASS without IDs, Jaccard vs Pass6 P80 0.481.*
